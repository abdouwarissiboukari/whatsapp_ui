import 'package:flutter/material.dart';

class StatusController extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Statut"),
    );
  }
}
